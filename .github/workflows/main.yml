name: my first workflow

on: [push , workflow_dispatch]

jobs:
  run-one-command:
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{env}}
      - run: echo "${{toJson(github)}}"
      - run: echo "${{secrets}}"
      - run: echo "${{vars}}"
      - run: echo "${{inputs}}"    
